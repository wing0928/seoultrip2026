# 實作紀錄

## 2026-07-29

- iPhone 17 驗證基準採用 402 × 874 CSS viewport；[Apple 官方規格](https://www.apple.com/iphone-17/specs/)列出的實體解析度為 2622 × 1206。
- 移除獨立交通頁與首頁四張捷徑卡；首頁改為「今日」，住宿與航班集中至設定。
- 手機底部導覽改為今日、行程、願望、地圖、更多；桌面保留完整分頁，所有頁面支援 hash 深層連結、瀏覽器返回與切頁回到頂端。
- 今日頁加入韓國標準時間、旅程倒數／旅行日、下一站、當日時間線與一鍵複製韓文名稱。
- 行程卡把導航提升為主要操作，Google Maps、來源、編輯與刪除收進次要選單；同一地點的連續項目不再顯示「導航到自己」。
- 行程與願望清單的刪除都加入綠色成功提示與六秒內復原。
- 願望清單加入中韓文／地區／備註搜尋、必去／未去／已去常用篩選、進階類型與地區篩選；空清單時不顯示多餘篩選器。
- 設定頁改為草稿模式，必須明確儲存或取消；旅行天數依日期自動推算。
- 新增 JSON 匯出、匯入摘要預覽與確認還原；測試資料已在操作後還原。
- 新增 PWA manifest、安裝圖示、Service Worker 與建置時 precache manifest，包含所有懶載入頁面，不需新增套件或付費服務。
- 共用景點類型、韓國時區、旅行日、備份與復原元件集中管理，移除未使用的頁面、舊首頁樣式、舊儲存函式、衍生 URL 持久化與不必要公開匯出。
- Naver 路線查詢移除英文地址清理後留下的重複標點。
- Google Places、Google Maps、Supabase 同步端點與呼叫時機保持不變，沒有調整 API 配額或新增付費功能。
- 舊 `trip_state` migration 仍保留，因為它可能已存在於遠端 migration history；在未稽核遠端資料前刪除會有資料遺失或 schema drift 風險。

### 驗證

- `npm test`：20 / 20 通過，涵蓋韓國時間、今日選擇、路線、備份、Naver 標點與離線 precache。
- `npm run build`：成功，主要 JS 442.09 kB（gzip 130.40 kB），其餘頁面拆為懶載入區塊。
- iPhone 17（402 × 874）與桌面（1440 × 900）完成視覺檢查。
- 實際操作完成：韓文複製、深層連結、返回鍵、行程刪除復原、設定取消／儲存／還原、JSON 備份還原、願望新增／搜尋／篩選／刪除復原。
- production manifest、圖示、Service Worker 與 precache manifest 均可取得；測試瀏覽器可離線重載 app shell。因內建瀏覽器不啟用 Service Worker 控制，完整懶載入離線流程另以 Service Worker 整合測試驗證。
- 將新生成的飛機圖套用為 Apple Touch Icon（180×180）與 PWA 安裝圖示（192×192、512×512），並將離線快取提升為 `seoul-trip-shell-v4`。

## 2026-08-01

- 願望清單與行程地點新增獨立 `CATCHTABLE` 連結欄位；卡片的「其他連結」與地圖詳情會顯示連結，大量貼上資料也會自動辨識 Catchtable URL。
- 搜尋改用顯示名稱與已知中韓翻譯組合，`꿉당 성수점` 現可用「烤堂」或「聖水店」找到；搜尋同時涵蓋備註、來源、CATCHTABLE 與服裝子標籤。
- 行程卡的簡介改為「顯示簡介」收合區塊；既有願望卡維持原本直接閱讀備註的行為。
- 新增行程時改為先選地區、再選類型，最後才列出符合條件的願望清單詳細地點；原本手動輸入／編輯流程仍保留。
- 類型清單移除「美食、逛街、拍照點」，男裝與女裝統一顯示為「服裝」；舊資料以顯示層相容轉換，不會刪除或覆寫原本手動的 `#` 地區標籤。
- 內建初始行程也同步改用「餐廳／景點／選物店」等現行分類；舊儲存資料仍保留相容顯示，避免分類標籤在新舊資料間混用。
- 服裝新增獨立 `#男裝／#女裝／#鞋子／#綜合` 子標籤，可複選並在願望與行程卡顯示；子標籤不會自動寫入原有地區欄位。

### 驗證

- `npm test`：25 / 25 通過，新增 CATCHTABLE 解析、烤堂搜尋、舊類型相容與內建行程分類測試。
- `npm run build`：成功。
- 本機瀏覽器實際操作：新增服裝測試地點、選擇地區／類型／子標籤、儲存備註與 CATCHTABLE、以「烤堂」搜尋、從行程先選地區與類型再帶入詳細地點、展開「顯示簡介」、確認行程保留 CATCHTABLE；測試地點已刪除並重新載入確認不再存在。

## 2026-08-02

### CATCHTABLE restaurant and cafe buttons

- Restaurant and cafe wishlist cards now show a CATCHTABLE button. Saved `catchtableUrl` values remain direct links; otherwise the app opens a CATCHTABLE site search.
- Search URLs are generated at render time and are not persisted as fake listings. Other place types stay blank, and an unmatched CATCHTABLE search remains an empty result.
- CATCHTABLE 搜尋改為只帶韓文／拉丁店名，不再把中文店名或中文地區附加到搜尋參數；沒有可辨識名稱時維持空白。
- CATCHTABLE 按鈕改為同一分頁導向，並將自動搜尋改用 CATCHTABLE Global 的 App-Link-safe 首頁 `www.catchtable.net/?keyword=...`；避免不支援的 `/search/` 路由造成 App 顯示 Page Not Found。
- Validation: `npm.cmd test` (30 passed) and `npm.cmd run build` (success).

- 頁首新增「重新整理」按鈕，使用瀏覽器重新載入目前頁面，不會清除行程、願望清單或其他 localStorage 資料。

### 驗證（重新整理按鈕）

- Build 後確認按鈕含 `aria-label="重新整理"`，並保留目前 URL 與既有資料儲存機制。

## 2026-08-02（願望景點備註遷移）

- 既有願望景點的 `note` 會在載入、備份還原與同步時遷移為 `description`，並清空願望景點備註；原有行程資料的 `note` 不會被遷移或覆寫。
- 從願望清單帶入行程時，只帶入景點簡介，不再把願望景點舊備註自動帶成行程備註，讓使用者可以在行程編輯器重新輸入當天備註。
- 願望清單仍保留「景點備註（選填）」欄位，提供未來需要額外記錄時使用；已有景點簡介與備註分開的資料不會互相覆蓋。

### 驗證（願望景點內容遷移）

- `npm test`：29 / 29 通過，新增 wishlist migration 測試，確認舊備註會移到簡介、行程備註資料不受影響。
- `npm run build`：成功。

## 2026-08-02（行程備註與景點簡介）

- 行程卡將「備註」與「景點簡介」分開：行程備註直接顯示在卡片內，景點簡介改由「顯示景點簡介」按鈕收合顯示；願望清單帶入行程與編輯表單均保留這兩個欄位。
- 未有景點簡介的既有資料不會被自動改寫；原有備註內容維持不變，避免把交通、營業時間或排隊提醒誤當成景點介紹。

### 驗證（行程內容分流）

- `npm test`：28 / 28 通過，新增描述欄位持久化測試。
- `npm run build`：成功。
- 本機瀏覽器確認：行程卡直接顯示「行程備註」；填入景點簡介後可透過「顯示景點簡介」展開，並已清除測試內容恢復原狀。

## 2026-08-02（導航與地圖距離）

- 「開始導航／查路線」改為以 Naver Maps 路線 URL Scheme 開啟：目的地先使用景點的韓文名稱，並以既有 Google Places 座標快取補上目的地座標；Naver Maps App 未安裝或座標無法取得時仍保留搜尋備援，不會把「大眾交通」等文字拼進店名查詢。
- Naver Maps App 路線省略出發座標，交由 Naver Maps 使用裝置目前位置；桌面版則開啟目前位置到目的地的 Naver 路線頁。沒有新增 Naver API 或付費服務。
- 地圖頁新增「顯示目前位置距離」按鈕，使用瀏覽器定位權限後，在每個景點名稱後顯示直線距離；定位拒絕、不支援或景點座標缺失時會提供清楚的狀態訊息，不影響原本地圖、搜尋與篩選。
- 距離採 Haversine 直線距離，未宣稱步行／大眾運輸路線距離；若需要精確行走時間，仍應交由 Naver Maps 路線頁計算。

### 驗證

- `npm test`：27 / 27 通過，新增 Naver 路線參數與距離格式化測試。
- `npm run build`：成功，地圖懶載入區塊含定位按鈕、距離顯示與路線備援。
- 測試資料未新增或寫入；定位功能只在使用者按下按鈕後請求瀏覽器權限，沒有背景持續追蹤。

### 下一階段建議

- 若家人不希望每次允許定位，可再加「記住上次位置」選項，但需先確認隱私與裝置共用情境。
- 若想顯示實際步行／地鐵距離，建議仍以 Naver Maps 的路線結果為準，避免自行增加新的地圖 API 配額。

### 下一階段建議

- 若要正式大量匯入 Catchtable，可再補一個來源 URL 格式提示或連結預覽，但目前不需要付費服務。
- 若未來要讓家人共同編輯，可先決定是否需要登入／同步權限，再評估現有 Supabase 同步流程；本階段未改動既有同步與 Google Places 配額。
